package com.example;

public class Main {
    public static void main(String[] args) {
        while (true) {
            System.out.println("App is running...");
            try {
                Thread.sleep(10000); // Keeps the app alive
            } catch (InterruptedException e) {
                Thread.currentThread().interrupt();
            }
        }
    }
}
